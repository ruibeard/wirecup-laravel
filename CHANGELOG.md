# Changelog

All notable changes to `laravel-wirecup` will be documented in this file.

## Unreleased

- Sync Laravel package behavior with the current Wirecup spec
- Treat badge and alert elements as neutral sketch elements
- Support route-style link targets like `settings`, `/dashboard`, and `product.cup`
- Remove in-repo Workbench scaffolding and simplify package development flow

## 0.1.0

- Initial release
- Auto-discovered `/wirecup` browser and `/wirecup/render/{path}` preview route
- Native PHP Wirecup renderer
- Configurable root directory, route prefix, middleware, and default file
- Testbench test coverage
